import { useAuth } from '../hooks/use-auth';
import useNotes from '../hooks/use-notes';
import useOwnedNotes from '../hooks/use-owned-notes';

const UsersNotes = () => {
    //const { auth } = useAuth();
    //const name = auth.username
    //onsole.log(name)
    //console.log(auth.username)
    //const { ownedNotes } = useOwnedNotes(auth.username);
    //console.log(ownedNotes)

    return (
        <ul>
          <li></li>
        </ul>
    );
};
export default UsersNotes;
